# Optional: Google Places API (New) for reliable place discovery.
# Enable "Places API (New)" in Google Cloud Console.
GOOGLE_PLACES_API_KEY=

# DuckDB file used for metadata + QuackIR FTS index
REVIEWS_DB_PATH=data/reviews.duckdb

# Playwright browser (set false to watch scraping in a visible window)
HEADLESS=true
